import random

how = 5  # Количество номеров в справочнике
kod = num = []
for i in range(how):
    kod_tmp = random.randint(100,999)
    id
kod = [random.randint(100, 999) for _ in range(how)]
print(kod)